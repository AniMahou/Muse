# Synthetic corpus generation. Training deps (torch) are deliberately separate —
# generating data should not require a 2GB install.
numpy>=2.0
opencv-python-headless>=4.9
Pillow>=10.0
fonttools>=4.50
uharfbuzz>=0.39
freetype-py>=2.4
