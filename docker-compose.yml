# Ports are deliberately NOT the defaults. Another project on this machine
# already runs MongoDB on 27017, and pointing Muse at localhost:27017 would
# silently read and write that unrelated database. Muse owns 27018 and 6380.
services:
  mongo:
    image: mongo:7
    container_name: muse-mongo
    restart: unless-stopped
    ports:
      - "${MONGO_PORT:-27018}:27017"
    volumes:
      - muse-mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: muse-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6380}:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - muse-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  muse-mongo-data:
  muse-redis-data:
