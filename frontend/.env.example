# Leave empty in development — Vite proxies /api and /socket.io to the backend,
# so the browser sees a single origin and there is no CORS to debug.
#
# Set this only when the frontend is deployed somewhere the API is not.
VITE_API_URL=
